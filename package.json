{
  "name": "k6-docker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/main.ts src/loadTest.ts --bundle --format=esm --platform=neutral --target=es2020 --outdir=dist --external:k6 --external:k6/* --sourcemap",
    "watch": "npm run build -- --watch"
  },
  "devDependencies": {
    "@types/k6": "^1.3.1",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.3"
  }
}
